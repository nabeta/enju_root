<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="<%= @locale -%>" lang="<%= @locale -%>">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title><%= t('activerecord.models.reserve') -%>: <%=h title_action_name -%></title>
  <%= render :partial => 'page/include' -%>
  <%- if controller.action_name == 'index' -%>
    <%- if @user -%>
      <%= auto_discovery_link_tag(:rss, user_reserves_url(@user.login, :format => :rss), :title => (t('reserve.user_reserve', :login_name => @user.login) + " (RSS)")) -%>
      <%= auto_discovery_link_tag(:atom, user_reserves_url(@user.login, :format => :atom), :title => (t('reserve.user_reserve', :login_name => @user.login) + " (Atom)")) -%>
    <%- else -%>
      <%= auto_discovery_link_tag(:rss, reserves_url(:format => :rss), :title => (t('reserve.library_group_reserve', :library_group_name => @library_group.display_name.localize) + " (RSS)")) -%>
      <%= auto_discovery_link_tag(:atom, reserves_url(:format => :atom), :title => (t('reserve.library_group_reserve', :library_group_name => @library_group.display_name.localize) + " (Atom)")) -%>
    <%- end -%>
  <%- end -%>
  <link rel="search" type="application/opensearchdescription+xml" href="<%= opensearch_url -%>" title="<%= @library_group.display_name.localize -%>" />
</head>
<body class="yui-skin-sam" id="yahoo-com">

<%= render :partial => 'page/header' -%>
<%= render :partial => 'page/menu' -%>

<div id="content">

<div style="color: green"><%= flash[:notice] -%></div>

<%= yield  -%>

</div>

<%= render :partial => 'page/footer' -%>

</body>
</html>
