<%= form_for(@manifestation_relationship_type) do |f| %>
  <% if @manifestation_relationship_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manifestation_relationship_type.errors.count, "error") %> prohibited this manifestation_relationship_type from being saved:</h2>

      <ul>
      <% @manifestation_relationship_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
