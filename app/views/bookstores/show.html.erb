<div id="submenu">
  <ul>
    <li><%= link_to t('page.edit'), edit_bookstore_path(@bookstore) -%></li>
    <li><%= link_to t('page.back'), bookstores_path -%></li>
    <li><%= link_to t('activerecord.models.order_list'), bookstore_order_lists_path(@bookstore) -%></li>
  </ul>
</div>

<div id="content_detail">
<h1 class="title"><%= t('page.showing', :model => t('activerecord.models.bookstore')) -%></h1>
<div id="content_list">
<p>
  <b><%= t('activerecord.attributes.bookstore.name') -%>:</b>
  <%= @bookstore.name -%>
</p>

<p>
  <b><%= t('activerecord.attributes.bookstore.zip_code') -%>:</b>
  <%= @bookstore.zip_code -%>
</p>

<p>
  <b><%= t('activerecord.attributes.bookstore.address') -%>:</b>
  <%= @bookstore.address -%>
</p>

<p>
  <b><%= t('activerecord.attributes.bookstore.telephone_number') -%>:</b>
  <%= @bookstore.telephone_number -%>
</p>

<p>
  <b><%= t('activerecord.attributes.bookstore.fax_number') -%>:</b>
  <%= @bookstore.fax_number -%>
</p>

<p>
  <b><%= t('activerecord.attributes.bookstore.url') -%>:</b>
  <%= link_to h(@bookstore.url), @bookstore.url -%>
</p>

<p>
  <b><%= t('activerecord.attributes.bookstore.note') -%>:</b>
  <%= @bookstore.note -%>
</p>

</div>
</div>
