<div id="submenu">
  <ul>
    <li><%= link_to t('activerecord.models.subscription'), subscription_path(@subscription) if @subscription -%>
    <li><%= link_to t('page.listing', :model => t('activerecord.models.expression')), subscription_expressions_path(@subscription) if @subscription -%>
  </ul>
</div>

<div id="content_detail">
<h1 class="title"><%= t('page.new', :model => t('activerecord.models.subscribe')) -%></h1>
<div id="content_list">

<%= error_messages_for :subscribe -%>

<%- form_for(@subscribe) do |f| -%>
  <p>
    <b><%= t('activerecord.models.subscription') -%></b><br />
    <%- if @subscription -%>
		  <%= link_to h(@subscription.title), @subscription -%>
      <%= f.hidden_field :subscription_id, :value => @subscription.id -%>
    <%- else -%>
      <%= f.text_field :subscription_id, :value => params[:subscription_id] -%>
    <%- end -%>
  </p>

  <p>
    <b><%= t('activerecord.models.expression') -%></b><br />
    <%- if @expression -%>
		  <%= link_to h(@expression.original_title), @expression -%>
      <%= f.hidden_field :expression_id, :value => @expression.id -%>
    <%- else -%>
      <%= f.text_field :expression_id, :value => params[:expression_id] -%>
    <%- end -%>
  </p>
  <p>
    <%= f.label t('activerecord.attributes.subscribe.start_on') -%><br />
    <%= f.date_select :start_on -%>
  </p>
  <p>
    <%= f.label t('activerecord.attributes.subscribe.end_on') -%><br />
    <%= f.date_select :end_on -%>
  </p>

  <p>
    <%= f.submit t('page.create') -%>
  </p>
<%- end -%>

</div>
</div>
