<div id="submenu">
  <ul>
    <li><%= link_to ('Add a work'), works_path -%></li>
  </ul>
</div>

<div id="content_detail">
  <%= render :partial => 'manifestations/manifestation_patrons_list' -%>
  
  <%- form_for :expressions, :url => manifestation_expressions_path(@manifestation), :html => {:method => 'get'} do -%>
    <p>
      <%= ('Search term') -%>: <%= text_field_tag 'query', h(@query) -%>
      <%- if_authorized?(:destroy, @manifestation) do -%>
        <%- if params[:mode] == 'add' -%>
          <%= radio_button_tag 'mode', 'show' -%> <%= t('show') -%>
          <%= radio_button_tag 'mode', 'add', :checked => true -%> <%= ('Add') -%>
        <%- else -%>
          <%= radio_button_tag 'mode', 'show', :checked => true -%> <%= t('show') -%>
          <%= radio_button_tag 'mode', 'add' -%> <%= ('Add') -%>
        <%- end -%>
      <%- end -%>
      <%= submit_tag ('Search') -%>
      <%= link_to ('Back to index'), manifestation_expressions_path(@manifestation) -%>
    </p>
  <%- end -%>

<table style="width:100%">
  <tr>
    <th><%=('Title') -%></th>
    <th><%= ('Language') -%></th>
    <th></th>
  </tr>
  
<%- for expression in @expressions -%>
  <tr class="line<%= cycle("0", "1") -%>">
    <td><%= link_to h(expression.original_title), expression -%>
    <%= expression_form_icon(expression.expression_form) -%></td>
    <td><%=h expression.language.display_name -%></td>
    <td>
      <%- if_authorized?(:destroy, expression) do -%>
        <%- if params[:mode] == "add" -%>
          <%= link_to ('Add'), new_manifestation_embody_path(@manifestation, :expression_id => expression.id) unless @manifestation.expressions.include?(expression) -%>
        <%- else -%>
          <%- embody = @manifestation.embodies.find_by_expression_id(expression.id) -%>
          <%= link_to t('destroy'), manifestation_embody_path(@manifestation, embody), :confirm => ('Are you sure?'), :method => :delete -%>
        <%- end -%>
      <%- end -%>
    </td>
  </tr>
<%- end -%>
</table>

<%= will_paginate(@expressions) -%>
</div>
