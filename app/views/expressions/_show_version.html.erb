  <h2 class="resource_title">
    [E]
    <%= @expression.original_title -%>
    <%= content_type_icon(@expression.content_type) -%>
  </h2>

  <p>(<%= l(@expression.versions.find(@version).created_at) -%>)</p>

  <p>
    <%= raw patrons_list(@expression.work.patrons, current_user) if @expression.work -%>
    <%= raw patrons_list(@expression.patrons, current_user) -%>
  </p>

  <div id="tabs">
    <ul>
      <%= render 'show_menu' %>
      <li><a href="#history"><em><%= t('page.history') -%></em></a></li>
    </ul>
    <%= render 'show_content' -%>
    <%= render 'history_list' -%>
  </div>
  <%= render 'page/tabview' -%>
