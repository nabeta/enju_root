<div id="submenu">
  <ul>
    <li><%= link_to ('Add a work'), works_path -%></li>
    <li><%= link_to ('New expression'), new_work_expression_path -%></li>
  </ul>
</div>

<div id="content_detail">
  <h1>
    [W]
    <%= link_to h(@work.original_title), @work -%>
  </h1>
  <p>
    <%= patrons_list(@work.patrons) -%>
  </p>

  <%- form_for :expressions, :url => work_expressions_path(@work), :html => {:method => 'get'} do -%>
    <p>
      <%= ('Search term') -%>: <%= text_field_tag 'query', h(@query) -%>
      <%- if_authorized?(:destroy, @work) do -%>
        <%- if params[:mode] == 'add' -%>
          <%= radio_button_tag 'mode', 'show' -%> <%= t('show') -%>
          <%= radio_button_tag 'mode', 'add', :checked => true -%> <%= ('Add') -%>
        <%- else -%>
          <%= radio_button_tag 'mode', 'show', :checked => true -%> <%= t('show') -%>
          <%= radio_button_tag 'mode', 'add' -%> <%= ('Add') -%>
        <%- end -%>
      <%- end -%>
      <%= submit_tag ('Search') -%>
      <%= link_to ('Back to index'), work_expressions_path(@work) -%>
    </p>
  <%- end -%>

<table style="width:100%">
  <tr>
    <th><%= ('Title') -%></th>
    <th><%= ('Language') -%></th>
  </tr>
  
<%- for expression in @expressions -%>
  <tr>
    <td>
      [E]
      <%= link_to h(expression.original_title), work_expression_path(@work, expression) -%>
      <%= expression_form_icon(expression.expression_form) -%><br />
      <%= patrons_list(expression.authors, :link => true) -%>
      <%= patrons_list(expression.patrons, :link => true) -%>
    </td>
    <td><%=h expression.language.display_name -%></td>
    <td>
      <%- if_authorized?(:destroy, expression) do -%>
        <%- if params[:mode] == "add" -%>
          <%= link_to ('Add'), new_work_reify_path(@work, :expression_id => expression.id, :mode => 'add') unless @work.expressions.include?(expression) -%>
        <%- else -%>
          <%- reify = @work.reifies.find_by_expression_id(expression.id) -%>
          <%= link_to t('destroy'), work_reify_path(@work, reify), :confirm => ('Are you sure?'), :method => :delete -%>
        <%- end -%>
      <%- end -%>
    </td>
  </tr>
<%- end -%>
</table>

<%= will_paginate(@expressions) -%>
</div>
