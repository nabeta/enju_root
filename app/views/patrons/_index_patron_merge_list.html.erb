<div id="submenu">
  <ul>
    <li><%= link_to ('Back to merge list'), @patron_merge_list -%>
  </ul>
</div>

<div id="content_detail">
<p>Patron(s) for <%= link_to h(@patron_merge_list.title), patron_merge_list_path(@patron_merge_list) -%></p>

<%- form_for :patrons, :url => patron_merge_list_patrons_path(@patron_merge_list), :html => {:method => 'get'} do -%>
<p>
  <%= ('Search term') -%>: <%= text_field_tag 'query', h(@query) -%>
  <%- if_authorized?(:destroy, @patron_merge_list) do -%>
    <%- if params[:mode] == 'add' -%>
      <%= radio_button_tag 'mode', 'show' -%> <%= t('show') -%>
      <%= radio_button_tag 'mode', 'add', :checked => true -%> <%= ('Add') -%>
    <%- else -%>
      <%= radio_button_tag 'mode', 'show', :checked => true -%> <%= t('show') -%>
      <%= radio_button_tag 'mode', 'add' -%> <%= ('Add') -%>
    <%- end -%>
  <%- end -%>
  <%= submit_tag ('Search') -%>
  <%= link_to ('Back to index'), patron_merge_list_patrons_path(@patron_merge_list) -%>
</p>
<%- end -%>

  <table style="width: 100%">
  <tr>
    <th><%= ('Patron name') -%></th>
    <th><%= ('Type') -%></th>
    <th><%= ('Other designation') -%></th>
    <th><%= ('Place') -%></th>
    <th><%= ('Date of birth') -%></th>
    <th><%= ('Date of death') -%></th>
  </tr>
  
<%- @patrons.each do |patron| -%>
  <tr class="line<%= cycle("0", "1") -%>">
    <td><%= link_to h(patron.full_name), patron_merge_list_patron_path(@patron_merge_list, patron) -%></td>
    <td><%=h patron.patron_type.display_name -%></td>
    <td><%=h patron.other_designation -%></td>
    <td><%=h patron.place -%></td>
    <td><%=h patron.date_of_birth -%></td>
    <td><%=h patron.date_of_death -%></td>
    <td>
      <%- if_authorized?(:destroy, patron) do -%>
        <%- if params[:mode] == 'add' -%>
          <%= link_to ('Add'), new_patron_merge_list_patron_merge_path(@patron_merge_list, :patron_id => patron.id) unless @patron_merge_list.patrons.include?(patron) -%>
        <%- else -%>
          <%- patron_merge = @patron_merge_list.patron_merges.find_by_patron_id(patron) -%>
          <%= link_to t('destroy'), patron_merge_list_patron_merge_path(@patron_merge_list, patron_merge), :confirm => ('Are you sure?'), :method => :delete -%>
        <%- end -%>
      <%- end -%>
    </td>
  </tr>
<%- end -%>
</table>

<%= will_paginate(@patrons) -%>
</div>
