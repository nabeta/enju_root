<div id="submenu">
  <ul>
    <li><%= link_to t('page.new', :model => t('activerecord.models.family')), new_expression_family_path(@expression) -%></li>
    <li><%= link_to ('Add a manifestation'), expression_manifestations_path(@expression) -%></li>
  </ul>
</div>

<div id="content_detail">

  <h1>
    [E]
    <%= link_to h(@expression.original_title), expression_path(@expression) -%>
  </h1>

<%- form_for :families, :url => expression_families_path(@expression), :html => {:method => 'get'} do -%>
<p>
  <%= t('page.search_term') -%>: <%= text_field_tag 'query', h(@query) -%>
  <%- if_authorized?(:destroy, @expression) do -%>
    <%- if params[:mode] == 'add' -%>
      <%= radio_button_tag 'mode', 'show' -%> <%= t('page.show') -%>
      <%= radio_button_tag 'mode', 'add', :checked => true -%> <%= t('page.add') -%>
    <%- else -%>
      <%= radio_button_tag 'mode', 'show', :checked => true -%> <%= t('page.show') -%>
      <%= radio_button_tag 'mode', 'add' -%> <%= t('page.add') -%>
    <%- end -%>
  <%- end -%>
  <%= submit_tag t('page.search') -%>
  <%= link_to t('page.back_to_index'), expression_families_path(@expression) -%>
</p>
<%- end -%>

<table id="index">
  <tr>
    <th><%= t('activerecord.attributes.family.full_name') -%></th>
    <th><%= t('activerecord.models.family_type') -%></th>
    <th><%= t('activerecord.attributes.family.other_designation') -%></th>
    <th><%= t('activerecord.attributes.family.place') -%></th>
    <th><%= t('activerecord.attributes.family.date_of_birth') -%></th>
    <th><%= t('activerecord.attributes.family.date_of_death') -%></th>
    <th></th>
  </tr>
<%- @families.each do |family| -%>
  <tr class="line<%= cycle("0", "1") -%>">
    <td><%= link_to h(family.full_name), family -%></td>
    <td><%=h family.family_type.display_name -%></td>
    <td><%=h family.other_designation -%></td>
    <td><%=h family.place -%></td>
    <td><%=h family.date_of_birth -%></td>
    <td><%=h family.date_of_death -%></td>
    <td>
      <%- if_authorized?(:destroy, family) do -%>
        <%- if params[:mode] == 'add' -%>
          <%= link_to t('page.add'), new_expression_realize_path(@expression, :family_id => family.id) unless @expression.families.include?(family) -%>
        <%- else -%>
          <%- realize = @expression.realizes.find_by_family_id(family.id) -%>
          <%= link_to t('page.destroy'), expression_realize_path(@expression, realize), :confirm => t('page.are_you_sure'), :method => :delete -%>
        <%- end -%>
      <%- end -%>
    </td>
  </tr>
<%- end -%>
</table>

<%= will_paginate(@families, :previous_label => "&#171; " + t('page.previous'), :next_label => "&#187; " + t('page.next')) -%>
</div>
