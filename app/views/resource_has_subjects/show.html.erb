<div id="sidebar">
  <ul>
    <li><%= link_to t('page.listing', :model => t('activerecord.models.resource_has_subject')), resource_has_subjects_path -%></li>
    <li><%= link_to t('page.listing', :model => t('activerecord.models.subject')), subjects_path -%></li>
  </ul>
</div>

<div class="inner">
<h1 class="title"><%= t('page.showing', :model => t('activerecord.models.resource_has_subject')) -%></h1>
<div class="inner">
<p>
  <b><%= t('activerecord.attributes.resource_has_subject.work') -%>:</b>
  <!-- TODO: Manifestation以外 -->
  <%= link_to h(@resource_has_subject.work.original_title), @resource_has_subject.work if @resource_has_subject.work -%>
</p>

<p>
  <b><%= t('activerecord.attributes.resource_has_subject.subject') -%>:</b>
  <%= link_to h(@resource_has_subject.subject.term), @resource_has_subject.subject -%>
</p>

</div>
</div>
