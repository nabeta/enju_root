<h1><%= t('inter_library_loan.listing') -%></h1>

<table id="index">
  <tr>
    <th><%= t('activerecord.models.item') -%></th>
    <th><%= t('page.library') -%></th>
    <th><%= ('Borrowing library') -%></th>
    <th><%= t('activerecord.attributes.inter_library_loan.shipped_at') -%></th>
    <th><%= t('activerecord.attributes.inter_library_loan.received_at') -%></th>
  </tr>

<%- for inter_library_loan in @inter_library_loans -%>
  <tr class="line<%= cycle("0", "1") -%>">
    <td>
      <%= link_to h(inter_library_loan.item.item_identifier), inter_library_loan.item -%>
      <br />
      (<%= link_to h(inter_library_loan.item.manifestation.original_title), inter_library_loan.item.manifestation -%>)
    </td>
    <td><%= link_to h(inter_library_loan.item.shelf.library.name), library_path(inter_library_loan.item.shelf.library.short_name) -%></td>
    <td><%= link_to h(inter_library_loan.borrowing_library.name), library_path(inter_library_loan.borrowing_library.short_name) -%></td>
    <td><%=h inter_library_loan.shipped_at -%></td>
    <td><%=h inter_library_loan.received_at -%></td>
    <%- if_authorized?(:destroy, inter_library_loan) do -%>
      <td><%= link_to t('page.show'), inter_library_loan -%></td>
      <td><%= link_to t('page.edit'), edit_inter_library_loan_path(inter_library_loan) -%></td>
      <td><%= link_to t('page.destroy'), inter_library_loan, :confirm => t('page.are_you_sure'), :method => :delete -%></td>
    <%- end -%>
  </tr>
<%- end -%>
</table>

<br />

<%- if_authorized?(:new, InterLibraryLoan) do -%>
  <%= link_to t('inter_library_loan.new'), new_inter_library_loan_path -%>
<%- end -%>
