<div id="submenu">
  <ul>
    <li><%= link_to ('Back to patrons'), item_patrons_path(@own.item) -%></li>
    <li><%= link_to ('Back to items'), patron_items_path(@own.patron) -%></li>
  </ul>
</div>

<div id="content_detail">
<p>
<b><%= ('Patron') -%>:</b>
  <%= link_to h(@own.patron.full_name), patron_path(@own.patron) -%>
</p>

<p>
<b><%= ('Item') -%>:</b>
  <%= link_to h(@own.item.item_identifier), item_path(@own.item) -%>
  (<%= link_to h(@own.item.manifestation.original_title), manifestation_path(@own.item.manifestation) -%>)
</p>

</div>
