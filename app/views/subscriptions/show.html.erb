<div id="submenu">
  <ul>
    <li><%= link_to t('page.edit'), edit_subscription_path(@subscription) -%></li>
    <li><%= link_to t('work.add'), subscription_works_path(@subscription) -%></li>
    <li><%= link_to t('page.back'), subscriptions_path -%></li>
  </ul>
</div>

<div id="content_detail">
<div id="content_list">
<p>
  <b><%= t('page.title') -%>:</b>
  <%= @subscription.title -%>
</p>

<p>
  <b><%= t('activerecord.attributes.subscription.note') -%>:</b>
  <%= @subscription.note -%>
</p>

<table class="index">
  <tr>
    <th><%= t('page.title') -%></th>
  </tr>
  <%- @subscription.works.each do |work| -%>
    <tr class="line<%= cycle("0", "1") -%>">
      <td>
        <strong><%= link_to h(work.original_title), work -%></strong>
        <br />
        <%= raw patrons_list(work.patrons) -%>
      </td>
    </tr>
  <%- end -%>
</table>

</div>
</div>
