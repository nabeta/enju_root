<h1><%= t('page.listing', :model => t('activerecord.models.create')) -%></h1>

<table id="index">
  <tr>
    <th><%= t('activerecord.models.patron') -%></th>
    <th><%= t('activerecord.models.work') -%></th>
  </tr>

<%- for create in @creates -%>
  <tr class="line<%= cycle("0", "1") -%>">
    <td><%= link_to h(create.patron.full_name), patron_path(create.patron) -%></td>
    <td><%= link_to h(create.work.original_title), work_path(create.work) -%></td>
    <%- if_authorized?(:destroy, create) do -%>
      <td><%= link_to t('page.show'), create -%></td>
      <td><%= link_to t('page.destroy'), create, :confirm => t('page.are_you_sure'), :method => :delete -%></td>
    <%- end -%>
  </tr>
<%- end -%>
</table>

<%- if_authorized?(:new, Create) do -%>
  <p><%= link_to t('page.new', :model => t('activerecord.models.create')), new_create_path -%></p>
<%- end -%>

<%= will_paginate(@creates, :previous_label => "&#171; " + t('page.previous'), :next_label => "&#187; " + t('page.next')) -%>
