<%=
events = @events
RiCal.Calendar do
  events.each do |event|
    event do
      description event.title
      dtstart     event.started_at
      dtend       event.ended_at
      location    event.library.address
    end
  end
end.to_s
-%>
