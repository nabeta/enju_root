<div id="sidebar">
  <ul>
    <li><%= link_to t('page.back_to', :model => t('activerecord.models.patron')), manifestation_patrons_path(@produce.manifestation) -%></li>
    <li><%= link_to t('page.back_to', :model => t('activerecord.models.manifestation')), patron_manifestations_path(@produce.patron) -%></li>
  </ul>
</div>

<div id="main">
<h1 class="title"><%= t('page.showing', :model => t('activerecord.models.produce')) -%></h1>
<div class="inner">
<p>
  <b><%= t('activerecord.models.patron') -%>:</b>
  <%=link_to h(@produce.patron.full_name), @produce.patron -%>
</p>

<p>
  <b><%= t('activerecord.models.manifestation') -%>:</b>
  <%= link_to h(@produce.manifestation.original_title), @produce.manifestation -%>
</p>

</div>
</div>
