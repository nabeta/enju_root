<h1>Listing user_checkout_stat_has_users</h1>

<table>
  <tr>
    <th>User stat</th>
    <th>User</th>
    <th>Checkouts count</th>
  </tr>

<% for user_checkout_stat_has_user in @user_checkout_stat_has_users %>
  <tr>
    <td><%=h user_checkout_stat_has_user.user_checkout_stat_id %></td>
    <td><%=h user_checkout_stat_has_user.user_id %></td>
    <td><%=h user_checkout_stat_has_user.checkouts_count %></td>
    <td><%= link_to t('page.show'), user_checkout_stat_has_user %></td>
    <td><%= link_to t('page.edit'), edit_user_checkout_stat_has_user_path(user_checkout_stat_has_user) %></td>
    <td><%= link_to t('page.destroy'), user_checkout_stat_has_user, :confirm => t('page.are_you_sure'), :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user_checkout_stat_has_user', new_user_checkout_stat_has_user_path %>
